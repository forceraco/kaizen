{
  "name": "apex-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "db:push": "pnpm --filter db db:push",
    "db:seed": "pnpm --filter db db:seed"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "next": "^16.2.9",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
